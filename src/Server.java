import java.util.ArrayList;
import java.util.HashMap;

public class Server {
    static HashMap<String, BankAccount> accounts = new HashMap<>();
    static HashMap<String, Bank> banks = new HashMap<>();
    static HashMap<String, Client> clients = new HashMap<>();

    public static void main(String[] args) {
        //Запускаем сервер
        //Принимаем и обрабатываем запросы клиентов.
    }
    //Функция запуска сервера. возвращает ip-адрес.
    //Функции обработки всех запросов
    //Создания счетов
    //Перевод, пополнение, снятие.
}